{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "pnpm storybook",
      "name": "Storybook",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
